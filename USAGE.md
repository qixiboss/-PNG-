# 使用文档（中文）

本工具是一个纯前端的图片处理工作流，包含三步：批量裁切 → 黑底转透明 → PNG 叠加合成。无需安装依赖，直接用浏览器打开 `index.html` 即可。

## 运行环境要求

- **推荐浏览器**：Chrome / Edge 86+（支持 `showDirectoryPicker` 文件夹保存）。
- **兼容方案**：若浏览器不支持文件夹保存，请使用页面上的 ZIP 导出功能。

## 快速开始

1. 在浏览器中打开 `index.html`。
2. 按页面引导依次使用：
   - **Step 1：批量图片裁切** → `batch-crop.html`
   - **Step 2：黑底转透明** → `bg-remove.html`
   - **Step 3：跑步数据叠加** → `data-overlay.html`

## Step 1：批量图片裁切（batch-crop.html）

1. 点击 **“选择图片（可多选）”**，导入待裁切图片。
2. 在右侧画布拖拽裁切框，或在左侧输入精确的 X/Y/宽/高。
3. 可用快捷按钮：
   - **重置裁切区域**：恢复与原图同大小
   - **居中正方形**：裁切为居中正方形
   - **适应图片**：与原图一致
4. 导出方式：
   - **一键导出 PNG**：保存到你选择的文件夹
   - **导出 ZIP**：打包下载

> 提示：裁切结果会自动写入浏览器的 LocalStorage，供下一步直接导入。

## Step 2：黑底转透明（bg-remove.html）

1. 点击 **“选择图片（可多选）”**，导入裁切后的图片。
2. 点击 **“预览第一张”**，先检查抠图效果。
3. 使用 **“从预览图取色”** 在图片中点击要变透明的颜色（默认黑色）。
4. 调整参数：
   - **颜色相似度阈值**：数值越大，透明范围越大
   - **边缘柔化**：消除锯齿，适当增加可更柔和
   - **尽量保留深色描边**：保留文字边缘（可按需关闭）
5. 导出方式：
   - **批量导出 PNG**：保存到选择的文件夹
   - **批量导出 PNG (ZIP)**：打包下载

> 提示：导出的透明 PNG 会自动写入 LocalStorage，供第三步直接导入。

## Step 3：PNG 图片合成（data-overlay.html）

1. 点击 **“底图设置”** 上传背景图（JPG/PNG/GIF）。
2. 点击 **“PNG图层管理”** 批量导入透明 PNG。
3. 主要操作：
   - **拖拽图层** 调整位置
   - **透明度** 滑杆调节透明度
   - **放大/缩小/重置** 控制图层尺寸
   - **滚轮缩放**：选中图层后，将鼠标放在图层上滚动缩放
   - **撤销/重做**：可回退/恢复操作
4. 点击 **“导出图片”** 导出最终合成 PNG。

## 常见问题

- **无法保存到文件夹**：浏览器不支持 `showDirectoryPicker`，请使用 ZIP 导出。
- **预览无显示**：请确认图片已加载完成，或重新点击“预览第一张”。
- **撤销/重做后图层不见**：请确认已完整导入图层，必要时重新导入。

## 数据存储说明

- 本工具使用 **LocalStorage** 传递 Step 1 → Step 2 → Step 3 的中间结果。
- 清理浏览器缓存或切换浏览器会导致自动导入失效。

---

如需更详细的功能说明，可查看 `README.md`。
